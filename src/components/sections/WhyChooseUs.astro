---
const differentiators = [
  {
    icon: 'shield_person',
    title: 'Technician Excellence',
    problem: 'Most companies send whoever is available.',
    solution:
      'Uniformed. Background-checked. 200+ hours of hands-on training — before they ever enter your home.',
  },
  {
    icon: 'nest_cam_iq_outdoor',
    title: 'Video Verification',
    problem: 'How do you know the work was actually done?',
    solution:
      'HD video footage of your chimney before and after. You see exactly what changed — with your own eyes.',
  },
  {
    icon: 'price_check',
    title: 'Upfront Pricing',
    problem: 'Surprise bills are the industry norm.',
    solution:
      'We give you a price, we stick to it. No hidden fees, no "discoveries" that double the bill. Ever.',
  },
  {
    icon: 'health_and_safety',
    title: 'Health-First Equipment',
    problem: 'Cheap equipment just moves dust around.',
    solution:
      'Hospital-grade HEPA filtration captures 99.97% of particles — including mold and allergens. Nothing gets recirculated.',
  },
];
---

<section class="differentiators section" id="difference" aria-label="Why Choose Us">
  <div class="differentiators__bg">
    <img
      src="/images/backgrounds/why-choose-home.webp"
      alt=""
      class="differentiators__bg-img"
      loading="lazy"
    />
    <div class="differentiators__bg-overlay"></div>
  </div>
  <div class="container">
    <div class="differentiators__layout">
      <div class="differentiators__intro" data-reveal>
        <span class="section-label">The 1st Class Difference</span>
        <h2 class="section-heading">Why 9 Out of 10 Customers Never Switch</h2>
        <p class="differentiators__description">
          We built our process around what homeowners actually hate about this industry — and
          eliminated it.
        </p>
      </div>
      <div class="differentiators__grid" data-reveal-group="80">
        {
          differentiators.map((item) => (
            <div class="differentiators__card" data-reveal>
              <div class="differentiators__icon-wrap">
                <span class="material-symbols-outlined">{item.icon}</span>
              </div>
              <div>
                <h4 class="differentiators__card-title">{item.title}</h4>
                <p class="differentiators__card-problem">{item.problem}</p>
                <p class="differentiators__card-desc">{item.solution}</p>
              </div>
            </div>
          ))
        }
      </div>
    </div>

    <!-- Micro-story (CRO Playbook §3) -->
    <div class="differentiators__story">
      <span class="differentiators__story-icon material-symbols-outlined">auto_stories</span>
      <blockquote class="differentiators__story-quote">
        "During a routine inspection, we discovered a 3-inch crack in the Hendersons' flue liner
        that could have caused a house fire within weeks. We showed them photos, explained their
        options, and had it repaired the same day."
      </blockquote>
      <cite class="differentiators__story-cite">
        — Mike T., Lead Technician · 12 years experience
      </cite>
    </div>
  </div>
</section>

<style>
  .differentiators {
    background-color: var(--color-charcoal);
    padding-top: var(--space-3xl);
    padding-bottom: var(--space-3xl);
    border-bottom: 1px solid rgba(255, 255, 255, 0.05);
    position: relative;
    overflow: hidden;
  }

  /* Subtle corner glow */
  .differentiators::before {
    content: '';
    position: absolute;
    top: -100px;
    right: -100px;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(216, 67, 21, 0.05) 0%, transparent 70%);
    pointer-events: none;
    z-index: 1;
  }

  .differentiators__bg {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .differentiators__bg-img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.08;
  }

  .differentiators__bg-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      var(--color-charcoal) 0%,
      rgba(26, 26, 26, 0.7) 50%,
      var(--color-charcoal) 100%
    );
  }

  .differentiators > .container {
    position: relative;
    z-index: 2;
  }

  .differentiators__layout {
    display: flex;
    flex-direction: column;
    gap: var(--space-2xl);
  }

  .differentiators__description {
    color: var(--color-text-muted);
    max-width: 440px;
    line-height: var(--line-height-relaxed);
  }

  .differentiators__time-note {
    display: flex;
    align-items: center;
    gap: var(--space-sm);
    margin-top: var(--space-md);
    font-size: var(--font-size-sm);
    color: var(--color-primary);
    font-weight: var(--font-weight-medium);
  }

  .differentiators__time-note .material-symbols-outlined {
    font-size: 18px;
  }

  .differentiators__grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--space-xl);
  }

  .differentiators__card {
    display: flex;
    gap: var(--space-md);
    transition: transform var(--transition-base);
  }

  .differentiators__card:hover {
    transform: translateY(-2px);
  }

  .differentiators__icon-wrap {
    flex-shrink: 0;
    width: 48px;
    height: 48px;
    border-radius: var(--radius-md);
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(216, 67, 21, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-primary);
  }

  .differentiators__card-title {
    color: var(--color-text-white);
    font-weight: var(--font-weight-bold);
    margin-bottom: var(--space-xs);
  }

  .differentiators__card-problem {
    font-size: var(--font-size-sm);
    color: #f87171;
    font-weight: var(--font-weight-medium);
    margin-bottom: var(--space-xs);
  }

  .differentiators__card-desc {
    font-size: var(--font-size-sm);
    color: var(--color-text-muted);
    line-height: var(--line-height-normal);
  }

  /* Micro-story */
  .differentiators__story {
    margin-top: var(--space-3xl);
    padding: var(--space-xl);
    background: rgba(216, 67, 21, 0.05);
    border: 1px solid rgba(216, 67, 21, 0.15);
    border-left: 4px solid var(--color-primary);
    border-radius: var(--radius-md);
    position: relative;
  }

  .differentiators__story-icon {
    position: absolute;
    top: var(--space-lg);
    right: var(--space-lg);
    font-size: 32px;
    color: var(--color-primary);
    opacity: 0.2;
  }

  .differentiators__story-quote {
    font-size: var(--font-size-lg);
    color: var(--color-text-light);
    font-style: italic;
    line-height: var(--line-height-relaxed);
    margin-bottom: var(--space-md);
    max-width: 700px;
  }

  .differentiators__story-cite {
    display: block;
    font-size: var(--font-size-sm);
    color: var(--color-primary);
    font-weight: var(--font-weight-bold);
    font-style: normal;
  }

  @media (min-width: 768px) {
    .differentiators__grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  @media (min-width: 1024px) {
    .differentiators__layout {
      flex-direction: row;
      align-items: flex-start;
      justify-content: space-between;
      gap: var(--space-3xl);
    }

    .differentiators__intro {
      flex: 0 0 36%;
      position: sticky;
      top: 100px;
    }

    .differentiators__grid {
      flex: 0 0 58%;
    }
  }
</style>
